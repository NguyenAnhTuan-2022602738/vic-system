# Data Directory

Thư mục này chứa dữ liệu price VIC.

- `raw/` — Dữ liệu thô từ vnstock (OHLCV)
- `processed/` — Dữ liệu đã xử lý (thêm technical indicators)

**Quy tắc**: Không chỉnh sửa file trong `raw/`. Chỉ đọc và tạo file mới trong `processed/`.
